import { Card, CardContent, CardHeader } from '@mui/material';

const OptionsTable = () => {

  return (
    <Card sx={{ boxShadow: 8, borderRadius: '4px', mt: 2 }}>
      <CardHeader title="Options" sx={{ pb: 0 }} />
      <CardContent>

      </CardContent>
    </Card>
  );
};

export default OptionsTable;